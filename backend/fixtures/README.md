# Fixtures

Hand-written capabilities the smoke scripts run.

Not recordings. These exist so a check of the deterministic machinery is a check
of *that* and nothing else: a recorded artifact would answer the machinery
question and the discovery question at once, so a failure would not say which
half broke. Ids are prefixed `fix_` to keep them distinct from anything the
catalog holds.
