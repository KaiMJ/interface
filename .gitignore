.env
*.env.local

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
dist/
build/

# Node
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

# Model weights — pulled into a docker volume on first run, never committed.
models/

# Evidence and artifacts are deliverables, so they are NOT ignored wholesale.
# Only scratch runs are.
evidence/scratch/
evidence/**/*.tmp
.gstack/
